meta {
  name: Cambiar Estado
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/orders/{{orderId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "PREPARANDO"
  }
}