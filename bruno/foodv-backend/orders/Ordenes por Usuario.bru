meta {
  name: Ordenes por Usuario
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/orders/user/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  userId: 2
}
